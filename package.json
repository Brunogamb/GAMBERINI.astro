{
  "name": "gamberini-astro",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && git checkout gh-pages && git rm -rf . && cp -r dist/* . && git add . && git commit -m 'deploy' && git push origin gh-pages && git checkout main"
  },
  "dependencies": {
    "@fontsource-variable/onest": "^5.2.9",
    "astro": "^5.11.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.14",
    "typescript": "^5.8.3"
  }
}
