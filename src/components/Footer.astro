---
import { Image } from 'astro:assets';
import logo from '../assets/img/logoblanco.webp';
---

<footer>
    <div class="footer-content">
        <div class="footer-grid">
            <div class="footer-column brand-col">
                <a href="/" class="footer-logo">
                    <Image src={logo} alt="Gamberini Logo" class="logo-full" />
                </a>
                <p class="brand-desc">Especialistas en sistemas de bombeo y protección contra incendios.</p>
                <div class="social-pills">
                    <a href="https://instagram.com/gamberinibombas" target="_blank" class="ig-link" rel="noopener noreferrer">
                        <i class="fab fa-instagram"></i> @gamberinibombas
                    </a>
                </div>
            </div>

            <div class="footer-column">
                <h3>Ubicación</h3>
                <p>Francisco Soler 1083</p>
                <p>Paraná, Entre Ríos</p>
            </div>

            <div class="footer-column cta-col">
                <h3>¿Necesitás asesoramiento?</h3>
                <p>Contactate con nuestras áreas de Ingeniería, Service o Administración.</p>
                <a href="/contacto" class="btn-footer">Contactar</a>
            </div>
        </div>

        <div class="footer-divider"></div>

        <div class="footer-bottom">
            <p class="copyright">&copy; {new Date().getFullYear()} Gamberini. Todos los derechos reservados.</p>
            <p class="dev">Desarrollado por <a href="https://github.com/BrunoGamb" target="_blank" rel="noopener noreferrer">BrunoGamb</a></p>
        </div>
    </div>
</footer>

<style>
footer {
    background: var(--brand-900);
    display: flex;
    flex-direction: column;
    align-items: center;
    color: var(--white);
    text-wrap: balance;
    padding: 10px 20px;
}

.footer-grid {
    display: grid;
    grid-template-columns: 1.5fr 1fr 1.5fr; 
    gap: 50px; 
}

h3 {
    text-transform: uppercase;
    font-size: 18px;
    margin-bottom: 8px;
    letter-spacing: 2px;
    font-weight: 700;
    color: var(--white);
}

.footer-content {
    max-width: 1200px;
    width: 100%;
    display: grid;
}

.footer-column {
    display: flex;
    flex-direction: column;
    align-self: flex-start;
    margin: 50px 0 30px 0;
    line-height: 1.6;
}

.footer-column p {
    font-size: 14px;
    line-height: 1.6;
    color: rgba(var(--white-rgb), 0.8);
    display: block;
    transition: 0.3s;
}

.location:hover a {
    color: var(--accent);
}

.footer-divider {
    width: 100%;
    max-width: 1200px;
    height: 1px;
    background: linear-gradient(to right, var(--accent) 0%, var(--white) 100%);
    opacity: 0.2;
    grid-column: 1 / -1;
    margin: 6px 0 10px;
}

.footer-bottom {
    display: flex;
    max-width: 1200px;
    width: 100%;
    justify-content: space-between;
    align-items: center;
    gap: 30px;
    margin: 8px auto;
    color: rgba(var(--white-rgb), 0.6);
    line-height: 1.4;
    font-size: 13px;
}

.footer-logo {
    display: flex;
    align-items: center;
    margin-bottom: 16px;
}

.footer-logo img {
    height: 26px;
    width: auto;
}

.dev a {
    font-weight: 600;
    font-size: 14px;
    border-bottom: 1px solid rgba(var(--white-rgb), 0.6);
}

.dev a:hover {
    color: var(--white);
    border-bottom-color: var(--white);
    border-bottom-width: 2px;
}

.copyright p {
    max-width: 350px;
    text-align: center;
}

.btn-footer, .social-pills a {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: transparent;
    color: var(--white);
    text-decoration: none;
    transition: all 0.2s ease;
    margin-top: 15px;
    font-weight: 600;
	padding: 10px 20px; 
    font-size: 14px; 
    text-transform: uppercase;
    letter-spacing: 1px;
    border-radius: var(--radius-sm);
    border: 1px solid var(--accent);
    color: rgba(var(--white-rgb), 0.8);
}

.footer-column a {
    width: fit-content;
    pointer-events: auto;
    white-space: nowrap;
}

.social-pills a i {
    font-size: 16px;
}

.btn-footer:hover, .social-pills a:hover {
    background: var(--brand-700);
    color: var(--white);
    box-shadow: var(--shadow-lg);
    border-color: var(--brand-700);
}

@media (max-width: 745px) {
    .footer-grid { 
        grid-template-columns: 1fr; 
        text-align: center;
        gap: 0;
    }

    .footer-column a, .footer-column p { 
        margin-left: auto; 
        margin-right: auto; 
    }

    .footer-column {
        margin: 20px 0;
    }

    .footer-bottom { 
        flex-direction: column; 
        gap: 15px; 
        text-align: center; }
}
</style>