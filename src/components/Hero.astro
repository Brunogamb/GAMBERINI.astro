---
import { Image } from 'astro:assets';

export interface Props {
    title: string;
    description: string;
    image: any;
    alt?: string;
}

const { title, description, image, alt } = Astro.props;
---

<section class="banner-hero" role="region" aria-labelledby="hero-heading">
    <div class="banner-hero-img">
        <Image src={image} alt={alt || title} />
        <div class="banner-hero-overlay" aria-hidden="true"></div>
        <div class="banner-text">
            <h1 id="hero-heading">{title}</h1>
            <p>{description}</p>
        </div>
    </div>
</section>

<style>

.banner-hero {
    overflow: hidden;
    box-shadow: var(--shadow-sm);
    margin: 0 auto 24px auto;
}

.banner-hero-img {
    position: relative;
    aspect-ratio: 16/6;
    max-height: 300px;
    width: 100dvw;
}

.banner-hero-img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
}

.banner-hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(90deg, rgba(var(--brand-900-rgb),0.88) 20%, rgba(var(--brand-800-rgb),0.88) 45%, rgba(var(--brand-700-rgb),0.88) 95%);
    opacity: 0.7;
    backdrop-filter: blur(var(--blur-sm));
}

.banner-text {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    z-index: 2;
    color: var(--white);
    text-align: center;
    word-break: break-word;
    text-wrap: balance;
}

.banner-text h1 {
    font-size: clamp(18px, 8vw, 38px);
    font-weight: 700;
    margin-bottom: 18px;
    line-height: 1.1;
}

.banner-text p {
    font-size: clamp(14px, 3vw, 20px);
    font-weight: 200;
    line-height: 1.5;
    color: var(--muted);
}

@media (max-width: 745px) {
    .banner-text {
        width: 100%;
    }

    .banner-text p{
        display: none;
    }

    .banner-text h1 {
        margin: auto;
    }
}
</style>